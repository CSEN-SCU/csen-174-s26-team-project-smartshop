/**
 * Future backend: orchestrate grocery search over store/catalog data (Express + local JSON/LowDB seam).
 * @param {object} _query
 * @returns {Promise<never>}
 */
export async function searchGroceryDeals(_query) {
  throw new Error("not implemented yet: searchGroceryDeals");
}
