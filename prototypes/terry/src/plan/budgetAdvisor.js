/**
 * Future backend helper: weekly savings target from spend profile. Stub for TDD—do not fully implement for Sprint 1.
 * @param {object} _profile
 * @returns {never}
 */
export function suggestWeeklySavingsTarget(_profile) {
  throw new Error("not implemented yet: suggestWeeklySavingsTarget");
}
