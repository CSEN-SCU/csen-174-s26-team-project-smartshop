# Copy to `.env.local` in this folder (never commit `.env.local`).

OPENAI_API_KEY=your_openai_api_key_here

# Required on Vercel: signs session + prototype profile cookies (same value across all deploys).
# Local dev: optional — falls back to smartshop-dev-auth-secret if unset.
AUTH_SECRET=your_random_secret_here
