/**
 * Find-similar-alternatives (test-driven — implement next to turn tests GREEN).
 * Stub only: keeps the suite runnable in RED phase.
 */

export async function postFindAlternatives(_openai, _description, _imageDataUrl = "") {
  throw new Error("postFindAlternatives not implemented yet");
}
